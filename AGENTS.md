# Ambient Glass agent notes

- Treat `design/reference/glance.png` as the visual source of truth and `PLAN.md` as the behavioral source of truth.
- Keep the wallpaper/preview background visually dominant; never turn the surface into a dashboard grid.
- Browser preview must work without credentials or Windows-only APIs.
- Do not expose credentials in frontend code, test fixtures, screenshots, or logs.
- Windows-specific Tauri behavior needs actual Dell validation before being marked verified.
